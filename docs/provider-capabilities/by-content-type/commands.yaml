# THIS FILE IS GENERATED. Do not edit directly.
# Source: docs/provider-capabilities/*.yaml
# Generated at: 2026-07-14T00:46:57Z

content_type: commands
providers:
    cline:
        supported: true
        capabilities:
            builtin_commands:
                supported: true
                mechanism: 5 built-in slash commands documented as individual headings (/newtask, /smol, /newrule, /deep-planning, /reportbug); hardcoded, not user-modifiable
                confidence: inferred
    factory-droid:
        supported: true
        capabilities:
            argument_substitution:
                supported: true
                mechanism: $ARGUMENTS substitution in Markdown commands plus positional shell args ($1, $2, ...) in Executable commands documented under '2 · Markdown commands' / '3 · Executable commands' / '5 · Usage patterns' headings
                confidence: inferred
    gemini-cli:
        supported: true
        capabilities:
            argument_substitution:
                supported: true
                mechanism: '{{args}} placeholder substitution plus !{...} shell-injection and @{...} file-injection escapes documented under ''Handling arguments'' / ''1. Context-aware injection with {{args}}'' / ''3. Executing shell commands with !{...}'' / ''4. Injecting file content with @{...}'' headings'
                confidence: inferred
    pi:
        supported: true
        capabilities:
            argument_substitution:
                supported: true
                mechanism: positional shell-style substitution ($1, $2, $@, $ARGUMENTS, ${@:N}, ${@:N:L}) documented under 'Argument Hints' / 'Arguments' / 'Usage' headings
                confidence: inferred
    windsurf:
        supported: true
schema_version: "1"
