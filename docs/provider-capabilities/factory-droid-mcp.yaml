provider: factory-droid
content_type: mcp
format: ""
proposed_mappings:
    - canonical_key: auto_approve
      supported: false
      mechanism: no auto-approve / always-allow field documented in Configuration Schema
      source_field: ""
      source_value: ""
      confidence: inferred
    - canonical_key: enterprise_management
      supported: true
      mechanism: org-managed 'Enterprise MCP policy' (mcpPolicy with enabled + allowlist of hostname/command matchers) restricts which servers may connect; 'MCP autonomy URL overrides' (mcpAutonomyUrlOverrides) assign per-URL autonomy risk levels; both delivered via org-managed settings and cannot be weakened downstream. A folder-level scope (.factory/mcp.json in any ancestor dir) also exists alongside user and project scopes
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: env_var_expansion
      supported: true
      mechanism: ${VAR} and ${VAR:-default} expansion in mcp.json documented under 'Variable expansion' heading (supported in stdio command/args/env and http url/headers; expanded in memory at load time, file never rewritten); stdio server add also accepts --env KEY=VALUE flags
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: marketplace
      supported: true
      mechanism: 'Quick Start: Add from Registry via /mcp command — 40+ pre-configured servers (Figma, Linear, Sentry, Notion, Supabase, Stripe, Vercel, Playwright, HubSpot, MongoDB) organized into categories'
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: oauth_support
      supported: true
      mechanism: OAuth authentication supported for HTTP servers; tokens stored globally in system keyring rather than per-project; managed via /mcp interactive manager
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: resource_referencing
      supported: false
      mechanism: no @-mention or resource referencing syntax for MCP resources documented
      source_field: ""
      source_value: ""
      confidence: inferred
    - canonical_key: tool_filtering
      supported: true
      mechanism: per-server tool filtering via disabledTools (blocklist) and enabledTools (allowlist; takes precedence when both set) fields in Configuration Schema, documented under 'Per-tool filtering' heading; mirrors --enabled-tools / --disabled-tools flags on droid exec
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: transport_types
      supported: true
      mechanism: 'three transports documented under ''Adding Servers via CLI'': http (Streamable HTTP, current standard, droid mcp add <name> <url> --type http), sse (legacy standalone HTTP+SSE, --type sse), and stdio (local processes, droid mcp add <name> ''<command>'' [--env KEY=VALUE...])'
      source_field: ""
      source_value: ""
      confidence: confirmed
