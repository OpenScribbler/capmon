provider: kiro
content_type: hooks
format: ""
proposed_mappings:
    - canonical_key: async_execution
      supported: false
      mechanism: Kiro hooks run synchronously; no async execution documented
      source_field: ""
      source_value: ""
      confidence: inferred
    - canonical_key: context_injection
      supported: true
      mechanism: Command hooks with exit code 0 add their STDOUT to the agent context for SessionStart and UserPromptSubmit events (ignored for other triggers); documented under 'Exit code behavior'
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: decision_control
      supported: true
      mechanism: 'Block-only: exit code 2 blocks execution for PreToolUse, UserPromptSubmit, and PreTaskExec (STDERR returned to the agent); documented via the ''Can block?'' column in the ''Trigger reference'' table and the ''Exit code behavior'' section. No allow or input-modify decisions documented'
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: handler_types
      supported: true
      mechanism: Hook action.type supports 'command' (shell) and 'agent' (predefined agent prompt) per the JSON file format — the agent type is a non-shell executor, which satisfies the canonical definition (executors beyond shell commands). No HTTP, mcp_tool, or TypeScript extension handler types documented
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: hook_scopes
      supported: false
      mechanism: Kiro hook JSON files live in .kiro/hooks/ at the workspace level only; no user/global/managed/plugin scope documented
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: input_modification
      supported: false
      mechanism: Kiro hooks cannot modify tool input before execution
      source_field: ""
      source_value: ""
      confidence: inferred
    - canonical_key: json_io_protocol
      supported: false
      mechanism: Hook config is JSON, but the command I/O protocol is exit-code + STDOUT/STDERR text (documented under 'Exit code behavior'), not a JSON stdin/stdout message protocol
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: matcher_patterns
      supported: true
      mechanism: Per-hook 'matcher' regex field (JSON file format) filters by tool name or file path; the 'Trigger reference' table 'Matcher matches' column documents Tool name (regex) for PreToolUse/PostToolUse and File path (regex) for PostFileCreate/PostFileSave/PostFileDelete
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: permission_control
      supported: false
      mechanism: Kiro hooks do not participate in permission decisions
      source_field: ""
      source_value: ""
      confidence: inferred
