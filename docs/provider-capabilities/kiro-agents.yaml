provider: kiro
content_type: agents
format: ""
proposed_mappings:
    - canonical_key: agent_scopes
      supported: true
      mechanism: project (.kiro/agents/) + global (~/.kiro/agents/); local (project) agent takes precedence over the global agent on name collision (with a warning message) — same workspace-wins model as other Kiro content types
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: definition_format
      supported: true
      mechanism: JSON config files in .kiro/agents/ (project) or ~/.kiro/agents/ (global); prompt field accepts inline string or file:// URI to external .md file
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: invocation_patterns
      supported: true
      mechanism: keyboard shortcut binding for IDE invocation; welcomeMessage shown on agent start; manual selection via agent picker
      source_field: ""
      source_value: ""
      confidence: inferred
    - canonical_key: model_selection
      supported: true
      mechanism: model field in agent JSON config overrides workspace default for that agent; falls back to default if specified model is unavailable
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: per_agent_mcp
      supported: true
      mechanism: inline mcpServers map per agent config; includeMcpJson boolean controls whether workspace .kiro/settings/mcp.json is merged into agent's server set
      source_field: ""
      source_value: ""
      confidence: confirmed
    - canonical_key: subagent_spawning
      supported: false
      mechanism: no subagent spawning mechanism documented for Kiro custom agents
      source_field: ""
      source_value: ""
      confidence: inferred
    - canonical_key: tool_restrictions
      supported: true
      mechanism: tools array with exact names, wildcard patterns (e.g. @server/read_*, @server/*_get), or MCP-namespaced refs (@server_name); allowedTools list for tools requiring no user approval; toolsSettings map for per-tool behavioral config
      source_field: ""
      source_value: ""
      confidence: confirmed
